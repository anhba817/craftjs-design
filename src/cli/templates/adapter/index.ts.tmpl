// __title__ adapter — generated by `crafted-design scaffold adapter`.
//
// An adapter maps the editor's canonical components onto a concrete design
// system. This skeleton wires two canonicals (box, button); the registry has
// 48 in total — implement the rest in ./components/ and add them to the
// `components` map below. Canonicals with no impl render a placeholder.
import { registerAdapter } from '@crafted-design/editor/sdk'
import { __Name__Wrapper } from './Wrapper'
import { __Name__BoxImpl } from './components/Box'
import { __Name__ButtonImpl } from './components/Button'

registerAdapter({
  id: '__id__',
  displayName: '__title__',
  // If this adapter wraps a library the host must install, declare it here so
  // the compatibility matrix and version-drift checks can see it, e.g.:
  //   peerDependencies: { 'your-ui-lib': '^1' },
  Wrapper: __Name__Wrapper,
  components: {
    box: __Name__BoxImpl,
    button: __Name__ButtonImpl,
    // …add the remaining canonicals here.
  },
})
