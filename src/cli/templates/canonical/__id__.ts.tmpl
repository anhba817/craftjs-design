// __title__ canonical — generated by `crafted-design scaffold canonical`.
//
// A canonical is a design-system-agnostic component definition: an id, a typed
// props schema, defaults, and which inspector panels apply. Adapters then
// provide the per-design-system rendering for this id. Requires `zod` (a
// dependency of the editor) for the props schema.
import { z } from 'zod'
import { registerComponent } from '@crafted-design/editor/sdk'

export const __Name__PropsSchema = z.object({
  label: z.string(),
})
export type __Name__Props = z.infer<typeof __Name__PropsSchema>

registerComponent<__Name__Props>({
  id: '__id__',
  category: 'display',
  displayName: '__title__',
  tags: ['__id__'],
  // Leaf component (no nested canvas). Set true for a container that holds
  // other nodes (and add a styleSlots/canvasSlots config).
  isCanvas: false,
  styleSlots: ['root'],
  propsSchema: __Name__PropsSchema,
  defaults: {
    props: { label: '__title__' },
    style: { classes: { root: '' } },
  },
  // Inspector panels offered for this canonical. Built-in ids: 'layout',
  // 'size', 'spacing', 'typography', 'appearance', 'effects', 'componentProps'.
  applicablePanels: ['spacing', 'size', 'appearance', 'effects', 'componentProps'],
})
